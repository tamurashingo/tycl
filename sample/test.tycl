;;;; Sample TyCL file for testing

(defun [add :integer] ([x :integer] [y :integer])
  "Add two integers"
  (+ x y))

(defun [greet :string] ([name :string])
  "Generate greeting message"
  (format nil "Hello, ~A!" name))

(let (([result :integer] (add 10 20)))
  (format t "Result: ~A~%" result))
